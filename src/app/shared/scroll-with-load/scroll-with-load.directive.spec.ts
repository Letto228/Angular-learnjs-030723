import {ScrollWithLoadDirective} from './scroll-with-load.directive';

describe('ScrollWithLoadDirective', () => {
    it('should create an instance', () => {
        const directive = new ScrollWithLoadDirective();

        expect(directive).toBeTruthy();
    });
});
